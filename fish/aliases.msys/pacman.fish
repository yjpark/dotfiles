alias acs 'pacman -Sl | grep -i'
# alias acp 'apt-cache-package'

alias apt-install 'pacman -S'
alias apt-remove 'pacman -R'
# alias apt-content 'echo TODO'
# alias apt-update ''
alias apt-upgrade 'pacman -Su'
