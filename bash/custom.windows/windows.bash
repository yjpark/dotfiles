export LC_ALL="en_US.UTF-8"
export LANG="en_US.UTF-8"

unalias grep

#Powerline is not working properly with color and cursor pos
#under conemu
#Seems bash_it's prompt is really slow under windows, so stick to powerline.
#export PROMPT_COMMAND="prompt_command"
