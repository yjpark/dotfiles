#!/usr/bin/env bash

dotnet tool update --global paket
dotnet tool update --global fake-cli
dotnet tool update --global dotnet-try
dotnet tool update --global dotnet-search
dotnet tool update --global dotnet-proj
dotnet tool update --global fabulous-cli
dotnet tool update --global Falanx.Tool
dotnet tool update --global dotMorten.OmdGenerator
dotnet tool update --global Amazon.Lambda.Tools
dotnet tool update --global femto
dotnet tool update --global ilspycmd
dotnet tool update --global fantomas-tool

